{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.ao-lore.example/evidence-selection-transaction-v0.1.schema.json",
  "title": "AO Lore Evidence Selection Transaction v0.1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "transaction_id",
    "proposal_id",
    "proposal_digest",
    "authorization_id",
    "authorization_digest",
    "candidate_id",
    "candidate_digest",
    "provenance_digest",
    "allowed_write_set_digest",
    "status",
    "committed_at",
    "transaction_digest",
    "candidate_creation",
    "legal_advice",
    "property_decision",
    "candidate_review",
    "candidate_decision",
    "promotion",
    "canonical_query",
    "provider",
    "credential",
    "private_data",
    "publication",
    "release",
    "deployment",
    "authority_advanced"
  ],
  "properties": {
    "schema_version": {"const": "ao.lore.evidence-selection-transaction.v0.1"},
    "transaction_id": {"$ref": "#/$defs/id"},
    "proposal_id": {"$ref": "#/$defs/id"},
    "proposal_digest": {"$ref": "#/$defs/digest"},
    "authorization_id": {"$ref": "#/$defs/id"},
    "authorization_digest": {"$ref": "#/$defs/digest"},
    "candidate_id": {"$ref": "#/$defs/id"},
    "candidate_digest": {"$ref": "#/$defs/digest"},
    "provenance_digest": {"$ref": "#/$defs/digest"},
    "allowed_write_set_digest": {"$ref": "#/$defs/digest"},
    "status": {"const": "committed"},
    "committed_at": {"$ref": "#/$defs/time"},
    "transaction_digest": {"$ref": "#/$defs/digest"},
    "candidate_creation": {"const": false},
    "legal_advice": {"const": false},
    "property_decision": {"const": false},
    "candidate_review": {"const": false},
    "candidate_decision": {"const": false},
    "promotion": {"const": false},
    "canonical_query": {"const": false},
    "provider": {"const": false},
    "credential": {"const": false},
    "private_data": {"const": false},
    "publication": {"const": false},
    "release": {"const": false},
    "deployment": {"const": false},
    "authority_advanced": {"const": false}
  },
  "$defs": {
    "id": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._-]{0,127}$"},
    "digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"},
    "time": {"type": "string", "format": "date-time", "maxLength": 32}
  }
}
